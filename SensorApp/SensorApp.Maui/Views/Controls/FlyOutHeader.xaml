<?xml version="1.0" encoding="utf-8" ?>
<StackLayout xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:utils="clr-namespace:SensorApp.Maui.Utils"
             x:Class="SensorApp.Maui.Views.Controls.FlyOutHeader"
             Padding="20"
             HorizontalOptions="Center"
             VerticalOptions="Center">

    <StackLayout Spacing="4" HorizontalOptions="Center">
        <Label Text="{Binding Username}" 
               FontSize="18" 
               FontAttributes="Bold" 
               HorizontalTextAlignment="Center" />

        <Label Text="{Binding Role, Converter={StaticResource EnumDisplayName}}" 
               FontSize="14" 
               TextColor="Gray"
               HorizontalTextAlignment="Center" />
    </StackLayout>
</StackLayout>